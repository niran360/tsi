name = "tsi-worker"
main = "src/index.js"
compatibility_date = "2023-10-30"

[vars]
# CLOUDFLARE_ACCOUNT_ID = ""
# CLOUDFLARE_API_TOKEN = ""
# Note: KV namespace is configured in production only.
# In local dev, the worker falls back to in-memory storage.
